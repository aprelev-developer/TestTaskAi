# QA-проверка блога 

Домашнее задание после занятия 1, **задание 4 (QA)**: «Проверка продукта,
который сделал агент».

Здесь нет разработки. Предмет работы — **проверка** существующего веб-приложения
и доказательства этой проверки: тест-план с ожиданиями, записанными до прогона,
баг-репорты с артефактами, автотест, падающий на реальных дефектах, фикс одного
бага с повторным прогоном и разбор того, где отчёт агента разошёлся с фактом.

---

## Почему выбрана эта тема

В задании прямо сказано, что свой проект предпочтительнее списка из семи тем.
Готовый проект был — PHP-блог, написанный в апреле 2026
(`github.com/aprelev-developer/blog-platform`).

Сдать его как основную реализацию нельзя: чек-лист требует «`SPEC.md` написан до
реализации, а не восстановлен после» и сохранённый наивный прогон. Проект
закончен четырьмя месяцами раньше и содержит два коммита — спецификация под него
была бы пересказом кода, а наивного прогона задним числом не бывает.

Зато задание 4 устроено так, что приложение появляется **раньше** тестировщика и
история его создания недоступна. Блог подошёл под это буквально. Предметом
спецификации стала тест-стратегия — и она честно написана до первой проверки.
Разбор этого решения — в `sessions/session-3.md` и `SPEC.md` §16.

---

## Как шла работа: шесть этапов курса и их следы в репозитории

`homework.pdf` (стр. 2) задаёт цикл из шести этапов, где каждый оставляет после
себя файл. Вот они и то, чем каждый закрыт здесь.

| Этап | Что осталось на диске | Коммит |
|---|---|---|
| **1. Контекст** | `AGENTS.md` Часть II: 10 категоричных запретов, 7 поводов остановиться и спросить, чек-лист из 9 пунктов, формат баг-репорта | `d6bf55b` |
| **2. Спецификация** | `SPEC.md` — 16 разделов, тест-план из 12 проверок с ожиданиями, 5 неоднозначностей, 12 критериев приёмки. Составлена по итогам интервью `grill-me`, 8 вопросов и ответов — в §15 | `d9eef2c` |
| **3. Наивный прогон** | `naive-run/naive-output.md` — один промпт, чистая сессия из нейтральной папки. Заморожен, после завершения не правился | `68b2694` |
| **4. Работа по спеке** | `qa/test-report.md`, `qa/bugs/` (4 баг-репорта), `qa/test_contract.py` (18 проверок, без зависимостей) | `321aa36`, `881745d` |
| **5. Проверка** | `qa/evidence/` — 3 скриншота и 6 логов; `qa/fix.diff` — фикс одного бага; `qa/audit-findings.md` и `qa/audit2-raw.md` — **два** независимых аудита в чистых сессиях | `d5f46e7`, `9a2dad8`, `138ab1d` |
| **6. Отчёт** | этот README, `sessions/session-3.md` (11 дословных промптов), `naive-run/COMPARISON.md`, `qa/discrepancies.md` (8 расхождений) | `2ad67ff`, `49ce078` |

**Порядок доказуем историей, а не словами.** `d9eef2c` (спецификация) идёт
раньше `321aa36` (первый отчёт о прогоне), а `68b2694` (наивный прогон) — между
ними. То есть спека написана до работы, а не восстановлена после: на момент её
коммита не было выполнено ни одной проверки из плана.

Ваш промпт задавал цикл строже курсового — с обязательным этапом `grill-me`
перед проектированием и с независимым аудитом отдельным шагом. Он сохранён
дословно в `sessions/session-3.md`, «Промпт 1».

---

## Как запустить

Нужны Docker и Python 3. Больше ничего ставить не нужно: автотест использует
только стандартную библиотеку.

### 1. Поднять объект тестирования

```bash
git clone --branch develop \
  https://github.com/aprelev-developer/blog-platform.git app-under-test
cd app-under-test
cp .env.example .env

# если порт 3306 на машине занят — скопировать готовый override из этого репозитория
cp ../docker-compose.override.yml .

docker compose build && docker compose up -d

# Дать MySQL прогреться. Без этой паузы следующий шаг может упасть:
# healthcheck отмечает базу готовой не мгновенно.
sleep 15

docker compose exec -T php composer install --no-interaction
for f in database/migrations/*.sql; do
  docker compose exec -T mysql mysql -ublog_user -pblog_password blog < "$f"
done
docker compose exec -T php php database/seeders/run.php
docker compose exec -T php sass resources/scss/main.scss public/css/style.css --no-source-map
cd ..
```

Занятыми могут оказаться и другие порты: **8080** (само приложение) и **8081**
(phpMyAdmin). Оверрайд снимает только 3306 — если конфликтует 8080 или 8081,
их придётся освободить или дописать в `docker-compose.override.yml`.

### 2. Убедиться, что стенд жив

```bash
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8080/   # ожидается 200
```

### 3. Прогнать автотест контракта

```bash
python3 qa/test_contract.py
```

**Тест обязан упасть.** На сдаваемом состоянии (ветка `develop`, без фикса)
ожидается **12 прошло / 6 упало**, код возврата `1`:

```
T05c   → BUG-004 (показана одна категория из нескольких)
T10    → BUG-001 (несогласованная валидация page)
T10b   → BUG-001 (несогласованная валидация page)
T09    → BUG-002 (ложное пустое состояние)
B003   → BUG-003 (HEAD отдаёт 405)
B003b  → BUG-003 (HEAD отдаёт 405)
```

Зелёный тест здесь означал бы, что он ничего не проверяет. Эталонный вывод —
`qa/evidence/autotest-final-develop.log`. На ветке `qa-fix/BUG-003` две проверки
`B003*` становятся зелёными: 14 / 4.

### 4. Остановить стенд

```bash
cd app-under-test && docker compose down
```

Пароли `blog_user` / `blog_password` — учебные значения из `.env.example` самого
объекта, а не секреты. Реальных ключей и токенов в репозитории нет.

---

## Что где лежит

| Путь | Что это |
|---|---|
| `SPEC.md` | Спецификация проверки: цель, сценарии, состояния, ограничения, нецели, тест-план из 12 проверок, неоднозначности, критерии приёмки |
| `AGENTS.md` | Правила проекта. Часть I — курса, не менялась. Часть II заполнена под эту задачу |
| `CLAUDE.md` | Точка входа Claude Code, подключает `AGENTS.md`. Не менялся |
| `qa/test-report.md` | Отчёт о прогоне: факт по каждой из 12 проверок и вердикт |
| `qa/bugs/` | Четыре баг-репорта |
| `qa/test_contract.py` | Автотест контракта, 18 проверок, без зависимостей |
| `qa/evidence/` | Скриншоты, скрипты проб, логи прогонов до и после фикса |
| `qa/fix.diff` | Правка кода объекта — единственная за всю работу |
| `qa/discrepancies.md` | Где отчёт разошёлся с проверкой, включая мои собственные ошибки |
| `qa/audit-findings.md` | Находки независимого аудита, включая отклонённые |
| `naive-run/naive-output.md` | Наивный прогон, заморожен |
| `naive-run/COMPARISON.md` | Сравнение двух прогонов |
| `sessions/` | Журнал работы: `session-1..3.md`, `STATE.md`, `TOOLS.md` |
| `app-under-test/` | Клон объекта. **В `.gitignore`**, в репозиторий не входит |

---

## Что найдено

| # | Дефект | Серьёзность | Чем доказан |
|---|---|---|---|
| BUG-001 | `page` валидируется несогласованно: `0` и `-1` → 400, а `abc`, пустое значение и 20-значное число молча открывают первую страницу | значительная | разбор кодов ответа |
| BUG-002 | Страница за пределами диапазона пишет «в категории нет постов» — при 8 постах в категории | **не**значительная (снижена аудитом) | скриншот |
| BUG-003 | `HEAD` на любую страницу отдаёт 405; отвечает само приложение (`X-Powered-By: PHP`) | значительная | разбор заголовков ответа |
| BUG-004 | Страница поста показывает одну категорию из нескольких: пост 4 есть в листингах `design` и `programming`, а объявляет только `design` | значительная | сравнение двух страниц, автотест T05c |

Три способа доказательства, и в `qa/test-report.md` объяснено, почему для
каждого дефекта другой способ не сработал бы: ложность фразы «постов нет» видна
только на экране рядом с заголовком категории; неверный код на `HEAD` браузер
пользователю не показывает вовсе; BUG-004 не ловится ни скриншотом, ни разбором
одной страницы — он существует только в отношении между листингом и карточкой.

**Починен один дефект** — BUG-003, в ветке `qa-fix/BUG-003` локального клона,
`qa/fix.diff`. Стенд сдаётся на `develop`, то есть **без** фикса: иначе шаги из
баг-репорта не воспроизводились бы. В `github.com/aprelev-developer` не
отправлено ничего.

**У фикса есть регрессия, и она названа.** После него HEAD маршрутизируется как
GET и потому инкрементирует счётчик просмотров: `22 views` → два запроса
`HEAD /post/7` → `25 views`. До фикса HEAD данные не менял. Правильное решение
сложнее однострочника — маршрутизировать HEAD как GET, но подавлять побочные
эффекты. Фикс оставлен как есть, с явной пометкой о регрессии: переделать его
и не проверить заново было бы хуже, чем назвать проблему.

---

## Что не сделано и что упрощено

Честно и без смягчений.

- **Требование «две ширины» не выполнено ни по одной.** `resize_window` дважды
  ответил «Successfully resized … to 360x800», но окно не изменилось. А
  оставшиеся скриншоты сняты шириной **1568 px**, а не 1440 — то есть проверена
  одна ширина, и та не та, что записана в плане. Имитировать 360 масштабированием
  я не стал: это выдало бы за проверку то, чем оно не является.
  Подробности — `qa/discrepancies.md`, D-1.
- **Независимый аудит нашёл 19 изъянов в моей же работе, и 14 из них я принял.**
  Три самых тяжёлых стоит назвать прямо здесь:
  - **Я объявил «подтверждённым» дефект, которого нет.** Находку наивного
    прогона «шаблон печатает сегодняшнюю дату вместо `published_at`» я записал
    как проверенную. На деле колонки `published_at` в проекте нет, а все 25
    постов созданы сидером в одну секунду — дата верна. Моя «перепроверка»
    проверяла следствие, одинаково согласующееся и с гипотезой, и с её
    отрицанием (`qa/discrepancies.md`, D-6).
  - **T05 должна была упасть, а я зачёл её пройденной.** Ожидание гласило
    «категории поста отображены» — во множественном числе. Я прочитал своё же
    ожидание в единственном. Дефект был **внутри** плана, а не за его
    границами, и моё красивое объяснение «фокус на входах не позволял увидеть
    этот класс» оказалось неверным (`qa/discrepancies.md`, D-7). Теперь это
    BUG-004.
  - **«Регрессий нет» после фикса — неправда**, см. выше про счётчик просмотров.
- **Наивный прогон всё равно выиграл по существу:** один подтверждённый дефект,
  которого нет у меня (BUG-004), плюс отсутствие заголовка `Allow` в ответах
  405, мимо которого прошёл мой BUG-003. Но он же выдал ложную находку HIGH с
  уверенной формулировкой — и я её принял. Сравнение —
  `naive-run/COMPARISON.md`.
- **Три дефекта из четырёх не починены** — BUG-001, BUG-002, BUG-004. По
  условию задания чинится один; остальные остаются как находки.
- **Наблюдения O-6…O-10 не доведены до баг-репортов:** несогласованность
  `/post/{id}` (`0` → 400, `-1` и `abc` → 404), слеш в конце → 404 вместо 301,
  регистрозависимые слаги, отсутствие заголовка `Allow`, и два случая, которые
  `SPEC.md` §13 сам записал в обязательные и которых в отчёте нет.
- **Проверка экранирования фактически не выполнена.** Вердикт был выставлен по
  входу, который отбрасывается маршрутизатором и до вывода не доходит, поэтому
  ничего не доказывает. Снят.
- **Данные стенда испорчены самими проверками:** счётчики просмотров выросли
  (`views [20,19,9,4,4,4,3,3]` при сиде «у всех 1»). Числа в логах
  невоспроизводимы без повторного прогона миграций и сидера.
- **Безопасность, доступность, кросс-браузерность, нагрузка — вне объёма**
  (`SPEC.md` §12). Проверено только экранирование и то, что `sort` не уходит в
  SQL. Утверждать «приложение безопасно» я не могу и не утверждаю.
- **Пагинация не проверена по-настоящему:** при сидовых данных максимум 8 постов
  в категории, и контролы пагинации не появляются ни на одной странице.
  Отличить «пагинация сломана» от «данных меньше, чем размер страницы» чёрным
  ящиком не удалось — зафиксировано как неоднозначность U5 в `SPEC.md` §10.
- **T03 прошла по случайности.** Ожидание «порядок соответствует убыванию
  просмотров» было записано без учёта того, что у всех постов 1 просмотр.
  Проверка сработала только потому, что мои же запросы накрутили счётчик одного
  поста. Ожидание в `SPEC.md` задним числом не переписано (`qa/discrepancies.md`, D-5).
- **`sessions/session-1.md` остался пустым шаблоном.** Работа фактически
  началась с сессии 2, до моего участия. Задним числом не заполнял.
- **Отклонение стенда от оригинала:** `docker-compose.override.yml` снимает
  публикацию порта 3306, потому что на машине занят чужой контейнер. Файлы
  объекта не изменялись.

---

## Ограничения проверки

- Проверка чёрным ящиком: исходный код объекта читался только для фикса BUG-003.
- Один браузер (Chrome), одна ширина (1440), одна конфигурация окружения.
- Данные — только сидовые: 6 категорий, 25 постов, у всех постов один просмотр
  на старте. Часть граничных случаев на таких данных недостижима.
- `APP_DEBUG=true`, как в `.env.example` объекта. Поведение в продакшен-режиме
  не проверялось.

---

## Ограничения проверки

- Проверка чёрным ящиком: исходный код объекта читался только для фикса BUG-003.
- Один браузер (Chrome), одна ширина (1440), одна конфигурация окружения.
- Данные — только сидовые: 6 категорий, 25 постов, у всех постов один просмотр
  на старте. Часть граничных случаев на таких данных недостижима.
- `APP_DEBUG=true`, как в `.env.example` объекта. Поведение в продакшен-режиме
  не проверялось.
